{
    "amount": {
        "required": "Payment amount is required",
        "min": "Payment amount cannot be bellow 1sh",
        "max": "Payment amount cannot be more than 50000sh"
    },
    "mode": {
        "required": "Payment mode is required",
        "values": "Payment mode contains an incorrect value"
    },
    "transaction-code": {
        "required": "Transaction code is required",
        "min": "Transaction code cannot be bellow 10 characters",
        "max": "Transaction code cannot be more than 10 characters",
        "pattern": "Transaction code can only contain letters and numbers",
        "unique": "Transaction code entered has already been recorded"
    }
}